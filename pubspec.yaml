name: avro_keyboard
description: Free Android system keyboard. Bengali, English, Avro phonetic, and device speech-to-text.
publish_to: "none"
version: 1.0.0+6

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
